-- ============================================
-- تعطيل RLS مؤقتاً للاختبار فقط
-- ⚠️ تحذير: هذا غير آمن للإنتاج!
-- استخدمه فقط للاختبار والتطوير
-- ============================================

-- تعطيل RLS على جدول services
ALTER TABLE services DISABLE ROW LEVEL SECURITY;

-- ============================================
-- بعد إصلاح المشكلة، أعد تفعيل RLS:
-- ============================================
-- ALTER TABLE services ENABLE ROW LEVEL SECURITY;
-- ثم شغّل ملف FIX_SERVICES_DELETE_RLS.sql

